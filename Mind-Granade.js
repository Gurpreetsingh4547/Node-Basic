const widht = 100;
const height = 100;

const calculateArea = (w, h) => console.log(w * h);

calculateArea(widht, height);

// Just importing some code by adding required modules
require("./Mind-Granade");